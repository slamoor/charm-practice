mainmodule hello {
    mainchare Main {
        // Main's entry methods
        entry Main(CkArgMsg *m);
        entry void done();
        entry void recv(long long n, int ind, int res);
    };
    array [1D] Hello {
        // Hello's entry methods
        entry Hello();
        entry void SayHi(long long n);
    };
};